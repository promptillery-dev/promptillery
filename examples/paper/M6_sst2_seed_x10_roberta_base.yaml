# =============================================================================
# M6 -- gold seed only, 10 rounds, default warmup (repeated-training control)
# =============================================================================
# GENERATED by scripts/gen_m6_configs.py from G3_sst2_ettin_encoder.yaml; re-run the generator
# instead of hand-editing. Default warmup on purpose: each of the 10 rounds trains exactly like the promptillery arm.
# Zero teacher calls (no prompt key). Output under out/m6. Not CI-verified.
# =============================================================================
name: m6_sst2_seed_x10_roberta_base
teacher: openrouter/openai/gpt-4.1
student: FacebookAI/roberta-base
student_type: transformers
dataset: SetFit/sst2
dataset_kwargs: {}
dataset_config:
  name: default
  num_classes: 2
  text_field: text
  label_field: label
metrics:
- accuracy
- f1
sampling:
  enabled: true
  sample_size: 1000
  train_ratio: 0.8
  stratify_column: label
  seed: 13
cycles: 10
seed: 13
learning_rate: 2e-5
num_train_epochs: 3
batch_size: 32
require_validation_split: true
token_budget: 20000000
budget_stop: true
base_output_dir: out/m6
early_stopping:
  enabled: false
trainer_config:
  trust_remote_code: true
  report_held_out_test: true
persist_datasets: false
auto_modify_name: false
