# =============================================================================
# M6 -- same-N gold fine-tune (1 cycle), warmup_steps 10
# =============================================================================
# GENERATED by scripts/gen_m6_configs.py from M5_imdb_same_n_ettin_encoder.yaml; re-run the generator
# instead of hand-editing. warmup_steps 10: the package default (500) never ends in a ~100-step run.
# Zero teacher calls (no prompt key). Output under out/m6. Not CI-verified.
# =============================================================================
name: m6_imdb_same_n_ettin_encoder
teacher: openrouter/openai/gpt-4.1
student: jhu-clsp/ettin-encoder-150m
student_type: transformers
dataset: json
dataset_kwargs:
  data_files:
    train: out/m5/imdb/ettin_encoder/train.jsonl
    validation: out/m5/imdb/validation.jsonl
    test: out/m5/imdb/test.jsonl
dataset_config:
  name: default
  num_classes: 2
  text_field: text
  label_field: label
metrics:
- accuracy
- f1
cycles: 1
seed: 13
learning_rate: 2e-5
num_train_epochs: 3
batch_size: 32
require_validation_split: true
base_output_dir: out/m6
early_stopping:
  enabled: false
trainer_config:
  trust_remote_code: true
  report_held_out_test: true
warmup_steps: 10
persist_datasets: false
auto_modify_name: false
