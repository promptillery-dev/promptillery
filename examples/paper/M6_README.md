# M6 encoder baselines (EACL 2027 resubmission) — Table 2 baseline rows

Generated by `scripts/gen_m6_configs.py`. Four variants per (dataset, student):
`ft1`, `same_n` (1 cycle, `warmup_steps: 10`), `same_n_cm`, `seed_x10` (10 rounds,
default warmup: the promptillery arm's exact per-round schedule). No teacher calls.

Data: run the M5 prep once per dataset (bootstraps every free prerequisite,
no API key): `uv run python scripts/prep_m5_same_n.py --dataset <D> --student ettin_encoder --nominal`.
RoBERTa reads the Ettin-encoder file (same seed, same N).

Run: `scripts/run_m6.sh examples/M6_sst2_ft1_roberta_base.yaml ...` (OOM ladder 32 → 16 → 8).
Seeds: `uv run python scripts/gen_m6_configs.py --only m6_sst2_ft1_roberta_base --seed 7` writes
`M6_sst2_ft1_roberta_base_s7.yaml` (name suffix `_s7`); Table 2 keeps seed 13.
Collect: `uv run python scripts/m6_results.py` → `docs/M6_RESULTS.md`, `docs/M6_TABLE2_ROWS.tex`.
