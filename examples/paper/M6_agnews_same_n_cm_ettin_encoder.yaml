# =============================================================================
# M6 -- same-N gold, compute-matched: 10 rounds, default warmup
# =============================================================================
# GENERATED by scripts/gen_m6_configs.py from M5_agnews_same_n_ettin_encoder.yaml; re-run the generator
# instead of hand-editing. Default warmup on purpose: each of the 10 rounds trains exactly like the promptillery arm.
# Zero teacher calls (no prompt key). Output under out/m6. Not CI-verified.
# =============================================================================
name: m6_agnews_same_n_cm_ettin_encoder
teacher: openrouter/openai/gpt-4.1
student: jhu-clsp/ettin-encoder-150m
student_type: transformers
dataset: json
dataset_kwargs:
  data_files:
    train: out/m5/agnews/ettin_encoder/train.jsonl
    validation: out/m5/agnews/validation.jsonl
    test: out/m5/agnews/test.jsonl
dataset_config:
  name: default
  num_classes: 4
  text_field: text
  label_field: label
metrics:
- accuracy
- f1
cycles: 10
seed: 13
learning_rate: 2e-5
num_train_epochs: 3
batch_size: 32
require_validation_split: true
base_output_dir: out/m6
early_stopping:
  enabled: false
trainer_config:
  trust_remote_code: true
  report_held_out_test: true
persist_datasets: false
auto_modify_name: false
