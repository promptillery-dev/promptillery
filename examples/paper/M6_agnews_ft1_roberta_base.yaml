# =============================================================================
# M6 -- fine-tuned (1 cycle) on the gold seed subset, warmup_steps 10
# =============================================================================
# GENERATED by scripts/gen_m6_configs.py from G3_agnews_ettin_encoder.yaml; re-run the generator
# instead of hand-editing. warmup_steps 10: the package default (500) never ends in a ~100-step run.
# Zero teacher calls (no prompt key). Output under out/m6. Not CI-verified.
# =============================================================================
name: m6_agnews_ft1_roberta_base
teacher: openrouter/openai/gpt-4.1
student: FacebookAI/roberta-base
student_type: transformers
dataset: SetFit/ag_news
dataset_kwargs: {}
dataset_config:
  name: default
  num_classes: 4
  text_field: text
  label_field: label
metrics:
- accuracy
- f1
sampling:
  enabled: true
  sample_size: 1000
  train_ratio: 0.8
  stratify_column: label
  seed: 13
cycles: 1
seed: 13
learning_rate: 2e-5
num_train_epochs: 3
batch_size: 32
require_validation_split: true
token_budget: 20000000
budget_stop: true
base_output_dir: out/m6
early_stopping:
  enabled: false
trainer_config:
  trust_remote_code: true
  report_held_out_test: true
warmup_steps: 10
persist_datasets: false
auto_modify_name: false
